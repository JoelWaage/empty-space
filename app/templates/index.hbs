<ul class="list-group row index-row container">
  {{#each model as |script|}}
    <ul class="box col-sm-3 index-display">
      {{script-tile script=script}}
    </ul>
  {{/each}}
</ul>

<div class="container">
  <button class="btn btn-lg btn-info cart-button">{{#link-to 'checkout'}}View Cart{{/link-to}}</button>
</div>
